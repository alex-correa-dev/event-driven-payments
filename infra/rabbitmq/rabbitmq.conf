mkdir -p infra/rabbitmq

cat > infra/rabbitmq/rabbitmq.conf << 'EOF'

management.tcp.port = 15672
management.tcp.ip = 0.0.0.0

vm_memory_high_watermark.relative = 0.8
disk_free_limit.relative = 1.0

consumer_timeout = 3000000
EOF